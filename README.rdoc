== Instagram Trail Search

Search for your favorite Smoky Mountain trail photos using the Instagram API.

* bundle install
* rake db:migrate
* rails s
* then visit http://localhost:3000/pages to search.

Now updated to handle nested JSON :).
